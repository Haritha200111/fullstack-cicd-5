import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<div style="font-family: Arial, Helvetica, sans-serif; padding: 2rem;">
    <h1>Angular + Go + Prisma (CI migrations)</h1>
    <p>Use the API at <code>/users</code> (backend).</p>
  </div>`,
})
export class AppComponent {}
